# --- Core ML ---
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.38.0
albumentations>=1.4.0

# --- Imagery / raster ---
# Avoid pinning GDAL directly here: on Windows, pip may try to build it from source
# and fail without the Visual C++ build tools. Rasterio/GeoPandas wheels are the
# preferred install path, and conda-forge is the fallback for systems that need
# a full GDAL runtime.
rasterio>=1.3.9
opencv-python-headless>=4.9.0
numpy>=1.26.0
Pillow>=10.0.0

# --- GIS / vector ---
geopandas>=0.14.0
shapely>=2.0.0
fiona>=1.9.0
pyproj>=3.6.0

# --- Metrics / utils ---
scikit-image>=0.22.0
scipy>=1.11.0
tqdm>=4.66.0

# --- Reporting (optional, used by results notebooks) ---
matplotlib>=3.8.0
